const fs=require('node:fs');fs.mkdirSync('dist',{recursive:true});for(const file of ['index.html','style.css','engine.js','game.js'])fs.copyFileSync(file,'dist/'+file);console.log('Built standalone HTML5 game in dist/');
